---
title: 'Write Python Code Directly In HTML Using PyScript'
date: '2022-05-06T23:46:37.121Z'
cover: './cover.png'
description: Steps to use PyScript to write python code directly in HTML
tags: ['python', 'html', 'javascript', 'featured']
slug: 'python-in-html-using-pyscript'
visibility: true
---

Do you want to write python code directly in HTML? Well, you can do that with <a href='https://pyscript.net/' target='_blank'> PyScript</a>.

How you ask? Just add these two lines inside the `<head>` tag of your HTML file like this:

```html
<html>
  <head>
    <link rel="stylesheet" href="https://pyscript.net/alpha/pyscript.css" />
    <script defer src="https://pyscript.net/alpha/pyscript.js"></script>
  </head>
</html>
```

and you are good to go 🚀

Below is the example usage of PyScript with output:

![PyScript Example Usage](pyscript-example.png 'PyScript Example Usage')

Cheers🍻 !!
